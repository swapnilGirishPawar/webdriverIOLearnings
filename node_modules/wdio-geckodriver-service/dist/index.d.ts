import type { Capabilities, Options } from '@wdio/types';
import type { GeckodriverServiceOptions } from './types';
export default class GeckoDriverService {
    options: GeckodriverServiceOptions;
    config: Options.Testrunner;
    private process?;
    private isMultiremote;
    private port?;
    private path;
    private geckodriverCustomPath?;
    private outputDir?;
    private logFileName;
    private args;
    constructor(options: GeckodriverServiceOptions, capabilities: Capabilities.RemoteCapability, config: Options.Testrunner);
    beforeSession(config: Omit<Options.Testrunner, 'capabilities'>, capabilities: Capabilities.RemoteCapability): Promise<void>;
    afterSession(): void;
    private _redirectLogStream;
    private _mapCapabilities;
    private _startDriver;
    private _stopDriver;
}
//# sourceMappingURL=index.d.ts.map